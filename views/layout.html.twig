<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="{{'https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet ' }} ">
    <link href="{{'http://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css'}}"  rel="stylesheet">
    <link href="{{ asset('/assets/css/style.css') }}" rel="stylesheet">
    <link href="{{ asset('/assets/css/bouton.css') }}" rel="stylesheet">
    <link href="{{ asset('/assets/css/comments.css') }}" rel="stylesheet">    
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top color zoom1">
    <div class="container-fluid">
          <div class="navbar-header">
              <button type="button" data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle">
                   <span class="icon-bar"></span>
                   <span class="icon-bar"></span>
                   <span class="icon-bar"></span> 
              </button>
              <span class="navbar-brand position"><img src="{{ asset('/assets/img/logo.png') }}"></span>
            <a href="{{ path('home') }}"><div class="navbar-brand fixation">Billet simple pour l'alaska </div></a>
            </div>
            <div class="collapse navbar-collapse">
              <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li>
                        <a href="{{path('about')}} ">Qui-suis je ?</a>
                    </li> 
                     {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li {% if adminMenu is defined %}active{% endif %}>
                       <a href="{{ path('admin') }}">Administration</a>
                    </li>
                    <li>
                        <a href="{{ path('logout') }}">Déconnexion</a>
                    </li>
                    {% endif %} {% else %}
                    <li>
                        <a href="{{ path('login') }}">Connexion</a>
                    </li>                   
                  {% endif %}     
              </ul>
        </div>
    </div>
</nav>
{% block content %}{% endblock %}
           
<script src="{{ asset('/assets/js/jquery.min.js') }}" type="text/javascript"></script>
<script src="{{ asset('/assets/js/bootstrap.min.js') }}" type="text/javascript"></script>
<script src="{{ asset('/assets/js/coulisse.js') }}" type="text/javascript"></script>

<!-- Control Center for Material Kit: activating the ripples, parallax effects, scripts from the example pages etc -->

</body>
</html>
